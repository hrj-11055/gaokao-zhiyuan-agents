# MVP 对话页实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现峰哥报考参谋 MVP 的首页 + AI 对话页，包含 SSE 流式输出、打字机效果和本地对话历史。

**Architecture:** UniApp (Vue 3) 前端 → Node.js Express 代理 → Dify API。前端通过 `uni.request` + `enableChunked` 接收 SSE 流，Node 代理保护 API Key 并转发请求。对话历史存储在 `uni.setStorageSync`。

**Tech Stack:** UniApp Vue 3 + Vite, Express.js, Dify Chat API (SSE)

**Spec:** `docs/superpowers/specs/2026-04-27-mvp-chat-design.md`

---

## File Map

```
gaokao-miniprogram/                    # UniApp 前端项目
├── package.json
├── vite.config.js
├── index.html
├── src/
│   ├── main.js                        # 应用入口
│   ├── App.vue                        # 根组件
│   ├── manifest.json                  # UniApp 应用配置
│   ├── pages.json                     # 页面路由
│   ├── uni.scss                       # 全局样式变量
│   ├── pages/
│   │   ├── index/
│   │   │   └── index.vue              # 首页
│   │   └── chat/
│   │       └── chat.vue               # 对话页
│   ├── components/
│   │   ├── ChatBubble.vue             # 消息气泡
│   │   └── QuickQuestions.vue          # 快捷问题
│   ├── api/
│   │   └── dify.js                    # Dify API 客户端 + SSE 解析
│   └── utils/
│       └── storage.js                 # 本地对话历史存储
└── static/

gaokao-proxy/                          # Node.js 代理
├── package.json
├── .env                               # Dify API Key + 端口
├── .env.example                       # 不含密钥的模板
└── server.js                          # Express 服务
```

---

### Task 1: UniApp 项目脚手架

**Files:**
- Create: `gaokao-miniprogram/package.json`
- Create: `gaokao-miniprogram/vite.config.js`
- Create: `gaokao-miniprogram/index.html`
- Create: `gaokao-miniprogram/src/main.js`
- Create: `gaokao-miniprogram/src/App.vue`
- Create: `gaokao-miniprogram/src/manifest.json`
- Create: `gaokao-miniprogram/src/pages.json`

- [ ] **Step 1: 创建项目目录和 package.json**

```bash
mkdir -p gaokao-miniprogram/src/{pages/index,pages/chat,components,api,utils,static}
```

```json
// gaokao-miniprogram/package.json
{
  "name": "gaokao-miniprogram",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:mp-weixin": "uni -p mp-weixin",
    "build:mp-weixin": "uni build -p mp-weixin"
  },
  "dependencies": {
    "@dcloudio/uni-app": "3.0.0-4060620250520001",
    "@dcloudio/uni-app-plus": "3.0.0-4060620250520001",
    "@dcloudio/uni-components": "3.0.0-4060620250520001",
    "@dcloudio/uni-h5": "3.0.0-4060620250520001",
    "@dcloudio/uni-mp-weixin": "3.0.0-4060620250520001",
    "vue": "^3.4.38"
  },
  "devDependencies": {
    "@dcloudio/uni-automator": "3.0.0-4060620250520001",
    "@dcloudio/uni-cli-shared": "3.0.0-4060620250520001",
    "@dcloudio/vite-plugin-uni": "3.0.0-4060620250520001",
    "vite": "^5.4.0",
    "sass": "^1.77.0"
  }
}
```

- [ ] **Step 2: 创建 vite.config.js**

```javascript
// gaokao-miniprogram/vite.config.js
import { defineConfig } from 'vite'
import uni from '@dcloudio/vite-plugin-uni'

export default defineConfig({
  plugins: [uni()]
})
```

- [ ] **Step 3: 创建 index.html**

```html
<!-- gaokao-miniprogram/index.html -->
<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>峰哥报考参谋</title>
    <!--preload-links-->
    <!--app-context-->
  </head>
  <body>
    <div id="app"><!--app-html--></div>
    <script type="module" src="/src/main.js"></script>
  </body>
</html>
```

- [ ] **Step 4: 创建 main.js 和 App.vue**

```javascript
// gaokao-miniprogram/src/main.js
import { createSSRApp } from 'vue'
import App from './App.vue'

export function createApp() {
  const app = createSSRApp(App)
  return { app }
}
```

```vue
<!-- gaokao-miniprogram/src/App.vue -->
<script setup>
import { onLaunch } from '@dcloudio/uni-app'

onLaunch(() => {
  console.log('App Launch')
})
</script>

<style>
page {
  background-color: #f5f5f5;
  font-family: -apple-system, BlinkMacSystemFont, 'Helvetica Neue', Helvetica, 'PingFang SC', 'Microsoft YaHei', sans-serif;
}
</style>
```

- [ ] **Step 5: 创建 manifest.json**

将 `mp-weixin.appid` 替换为实际的微信小程序 AppID。

```json
// gaokao-miniprogram/src/manifest.json
{
  "name": "峰哥报考参谋",
  "appid": "",
  "description": "张雪峰风格的 AI 志愿填报助手",
  "versionName": "0.1.0",
  "versionCode": "100",
  "transformPx": false,
  "mp-weixin": {
    "appid": "__YOUR_APPID__",
    "setting": {
      "urlCheck": false,
      "es6": true,
      "minified": true
    },
    "usingComponents": true
  }
}
```

- [ ] **Step 6: 创建 pages.json**

```json
// gaokao-miniprogram/src/pages.json
{
  "pages": [
    {
      "path": "pages/index/index",
      "style": {
        "navigationBarTitleText": "峰哥报考参谋",
        "navigationStyle": "custom"
      }
    },
    {
      "path": "pages/chat/chat",
      "style": {
        "navigationBarTitleText": "峰哥报考参谋",
        "navigationBarBackgroundColor": "#FFFFFF",
        "navigationBarTextStyle": "black"
      }
    }
  ],
  "globalStyle": {
    "navigationBarTextStyle": "black",
    "navigationBarTitleText": "峰哥报考参谋",
    "navigationBarBackgroundColor": "#FFFFFF",
    "backgroundColor": "#F9FAFB"
  }
}
```

- [ ] **Step 7: 创建占位页面**

```vue
<!-- gaokao-miniprogram/src/pages/index/index.vue -->
<template>
  <view class="container">
    <text>首页占位</text>
  </view>
</template>
```

```vue
<!-- gaokao-miniprogram/src/pages/chat/chat.vue -->
<template>
  <view class="container">
    <text>对话页占位</text>
  </view>
</template>
```

- [ ] **Step 8: 安装依赖并验证编译**

```bash
cd gaokao-miniprogram && npm install
npm run dev:mp-weixin
```

在微信开发者工具中打开 `gaokao-miniprogram/dist/dev/mp-weixin` 目录，确认两个页面都能正常显示。

- [ ] **Step 9: 提交**

```bash
cd ..  # 回到项目根目录
echo "node_modules/\ndist/\n.DS_Store\n.superpowers/" > .gitignore
git add .gitignore gaokao-miniprogram/
git commit -m "feat: scaffold UniApp miniprogram project"
```

---

### Task 2: 全局样式与品牌主题

**Files:**
- Create: `gaokao-miniprogram/src/uni.scss`

- [ ] **Step 1: 创建全局样式变量**

```scss
// gaokao-miniprogram/src/uni.scss

// 品牌色
$brand-primary: #F97316;
$brand-primary-light: #FDBA74;
$brand-primary-dark: #EA580C;
$brand-gradient-start: #FFF7ED;
$brand-gradient-end: #FEF3C7;

// 文字色
$text-primary: #1F2937;
$text-secondary: #4B5563;
$text-muted: #9CA3AF;

// 背景色
$bg-page: #F9FAFB;
$bg-white: #FFFFFF;
$bg-input: #F3F4F6;

// 边框
$border-light: #E5E7EB;

// 圆角
$radius-sm: 8rpx;
$radius-md: 16rpx;
$radius-lg: 24rpx;
$radius-xl: 32rpx;
$radius-full: 999rpx;

// 间距
$spacing-xs: 8rpx;
$spacing-sm: 16rpx;
$spacing-md: 24rpx;
$spacing-lg: 32rpx;
$spacing-xl: 48rpx;
```

- [ ] **Step 2: 验证样式变量可用**

在 `pages/index/index.vue` 中临时使用一个变量确认编译通过：

```vue
<template>
  <view class="container">
    <text class="test">样式测试</text>
  </view>
</template>

<style lang="scss" scoped>
.test {
  color: $brand-primary;
}
</style>
```

编译运行，确认文字显示为橙色。

- [ ] **Step 3: 提交**

```bash
git add gaokao-miniprogram/src/uni.scss
git commit -m "feat: add global brand styles and theme variables"
```

---

### Task 3: Node 代理服务器

**Files:**
- Create: `gaokao-proxy/package.json`
- Create: `gaokao-proxy/server.js`
- Create: `gaokao-proxy/.env`
- Create: `gaokao-proxy/.env.example`

- [ ] **Step 1: 创建代理项目**

```bash
mkdir -p gaokao-proxy
```

```json
// gaokao-proxy/package.json
{
  "name": "gaokao-proxy",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  }
}
```

- [ ] **Step 2: 创建 .env 文件**

```
# gaokao-proxy/.env
DIFY_API_URL=http://127.0.0.1:8080
DIFY_API_KEY=app-xxxxxxxxxxxxxxxx
PORT=3001
```

```bash
# gaokao-proxy/.env.example
# DIFY_API_URL=http://127.0.0.1:8080
# DIFY_API_KEY=app-xxxxxxxxxxxxxxxx
# PORT=3001
```

- [ ] **Step 3: 实现 server.js**

```javascript
// gaokao-proxy/server.js
require('dotenv').config()
const express = require('express')
const cors = require('cors')

const app = express()
app.use(cors())
app.use(express.json())

const DIFY_API_URL = process.env.DIFY_API_URL || 'http://127.0.0.1:8080'
const DIFY_API_KEY = process.env.DIFY_API_KEY
const PORT = process.env.PORT || 3001

if (!DIFY_API_KEY) {
  console.error('ERROR: DIFY_API_KEY is not set in .env')
  process.exit(1)
}

// Blocking mode
app.post('/api/chat', async (req, res) => {
  try {
    const { query, conversation_id = '', user } = req.body

    if (!query || !user) {
      return res.status(400).json({ error: 'query and user are required' })
    }

    const response = await fetch(`${DIFY_API_URL}/v1/chat-messages`, {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${DIFY_API_KEY}`,
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        inputs: {},
        query,
        response_mode: 'blocking',
        conversation_id,
        user
      })
    })

    if (!response.ok) {
      const errText = await response.text()
      console.error('Dify blocking error:', response.status, errText)
      return res.status(502).json({ error: '服务暂时不可用，请稍后再试' })
    }

    const data = await response.json()
    res.json(data)
  } catch (err) {
    console.error('Proxy error:', err.message)
    res.status(500).json({ error: 'AI 思考时间有点长，请稍后再试' })
  }
})

// Streaming mode — pipe SSE
app.post('/api/chat/stream', async (req, res) => {
  try {
    const { query, conversation_id = '', user } = req.body

    if (!query || !user) {
      return res.status(400).json({ error: 'query and user are required' })
    }

    const response = await fetch(`${DIFY_API_URL}/v1/chat-messages`, {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${DIFY_API_KEY}`,
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        inputs: {},
        query,
        response_mode: 'streaming',
        conversation_id,
        user
      })
    })

    if (!response.ok) {
      const errText = await response.text()
      console.error('Dify streaming error:', response.status, errText)
      return res.status(502).json({ error: '服务暂时不可用，请稍后再试' })
    }

    res.setHeader('Content-Type', 'text/event-stream')
    res.setHeader('Cache-Control', 'no-cache')
    res.setHeader('Connection', 'keep-alive')

    const reader = response.body.getReader()
    const decoder = new TextDecoder()

    const pump = async () => {
      while (true) {
        const { done, value } = await reader.read()
        if (done) {
          res.end()
          return
        }
        res.write(decoder.decode(value, { stream: true }))
      }
    }

    pump().catch((err) => {
      console.error('Stream pipe error:', err.message)
      if (!res.headersSent) {
        res.status(500).json({ error: 'AI 思考时间有点长，请稍后再试' })
      } else {
        res.end()
      }
    })
  } catch (err) {
    console.error('Proxy error:', err.message)
    if (!res.headersSent) {
      res.status(500).json({ error: 'AI 思考时间有点长，请稍后再试' })
    }
  }
})

// Health check
app.get('/api/health', (req, res) => {
  res.json({ status: 'ok' })
})

app.listen(PORT, () => {
  console.log(`Proxy server running on port ${PORT}`)
  console.log(`Dify API: ${DIFY_API_URL}`)
})
```

- [ ] **Step 4: 安装依赖并本地测试**

```bash
cd gaokao-proxy && npm install
npm run dev
```

在另一个终端测试健康检查：

```bash
curl http://localhost:3001/api/health
# 期望: {"status":"ok"}
```

- [ ] **Step 5: 测试 streaming 端点**

将 `.env` 中的 `DIFY_API_KEY` 设为真实 Key（如果服务器上有 Dify），或暂时跳过此步。

```bash
curl -N -X POST http://localhost:3001/api/chat/stream \
  -H "Content-Type: application/json" \
  -d '{"query":"你好","user":"test_user_001"}'
# 期望: SSE 流式输出（event: message / data: {...}）
```

- [ ] **Step 6: 提交**

```bash
cd ..  # 回到项目根目录
echo "gaokao-proxy/node_modules/" >> .gitignore
echo "gaokao-proxy/.env" >> .gitignore
git add .gitignore gaokao-proxy/
git commit -m "feat: add Node proxy server for Dify API"
```

---

### Task 4: 本地对话历史存储

**Files:**
- Create: `gaokao-miniprogram/src/utils/storage.js`

- [ ] **Step 1: 实现 storage.js**

```javascript
// gaokao-miniprogram/src/utils/storage.js

const STORAGE_KEY = 'chat_history'
const USER_ID_KEY = 'user_id'

/**
 * 获取或创建用户 ID（本地生成，无需微信登录）
 */
export function getUserId() {
  let userId = uni.getStorageSync(USER_ID_KEY)
  if (!userId) {
    userId = 'user_' + Date.now() + '_' + Math.random().toString(36).substring(2, 11)
    uni.setStorageSync(USER_ID_KEY, userId)
  }
  return userId
}

/**
 * 加载对话历史
 * @returns {{ conversationId: string, messages: Array, updatedAt: number }}
 */
export function loadHistory() {
  const data = uni.getStorageSync(STORAGE_KEY)
  if (!data) {
    return { conversationId: '', messages: [], updatedAt: 0 }
  }
  try {
    return JSON.parse(data)
  } catch {
    return { conversationId: '', messages: [], updatedAt: 0 }
  }
}

/**
 * 保存对话历史
 * @param {string} conversationId
 * @param {Array} messages - [{ role: 'user'|'ai', content: string, timestamp: number }]
 */
export function saveHistory(conversationId, messages) {
  const data = JSON.stringify({
    conversationId,
    messages,
    updatedAt: Date.now()
  })
  uni.setStorageSync(STORAGE_KEY, data)
}

/**
 * 追加一条消息并保存
 * @param {string} conversationId
 * @param {{ role: string, content: string, timestamp: number }} message
 * @returns {Array} 更新后的 messages 数组
 */
export function appendMessage(conversationId, message) {
  const history = loadHistory()
  const messages = [...history.messages, { ...message, timestamp: Date.now() }]
  saveHistory(conversationId, messages)
  return messages
}

/**
 * 清空对话历史
 */
export function clearHistory() {
  uni.removeStorageSync(STORAGE_KEY)
}
```

- [ ] **Step 2: 验证存储功能**

在 `pages/index/index.vue` 中临时添加按钮测试：

```vue
<template>
  <view class="container">
    <text>存储测试</text>
    <button @click="testStorage">测试存储</button>
  </view>
</template>

<script setup>
import { getUserId, saveHistory, loadHistory, clearHistory } from '../../utils/storage'

function testStorage() {
  const uid = getUserId()
  console.log('User ID:', uid)
  saveHistory('conv_123', [{ role: 'user', content: 'test', timestamp: Date.now() }])
  const history = loadHistory()
  console.log('History:', JSON.stringify(history))
  clearHistory()
  console.log('After clear:', JSON.stringify(loadHistory()))
  uni.showToast({ title: '存储测试通过', icon: 'success' })
}
</script>
```

在微信开发者工具中运行，点击按钮，控制台输出应包含 user_id 和历史记录。

- [ ] **Step 3: 提交**

```bash
git add gaokao-miniprogram/src/utils/storage.js
git commit -m "feat: add chat history storage utility"
```

---

### Task 5: Dify API 客户端 + SSE 解析

**Files:**
- Create: `gaokao-miniprogram/src/api/dify.js`

- [ ] **Step 1: 实现 SSE 解析器和 API 客户端**

```javascript
// gaokao-miniprogram/src/api/dify.js

// 后端代理地址（部署后替换为真实域名）
const API_BASE = 'http://localhost:3001'

/**
 * SSE 流式解析器
 * 将 onChunkReceived 收到的 ArrayBuffer 片段解析为 Dify 事件
 */
export class SSEParser {
  constructor(onMessage, onEnd, onError) {
    this.buffer = ''
    this.onMessage = onMessage
    this.onEnd = onEnd
    this.onError = onError
  }

  feed(chunkText) {
    this.buffer += chunkText

    // 按 \n\n 分割完整的 SSE 事件块
    const blocks = this.buffer.split('\n\n')
    this.buffer = blocks.pop() // 最后一个可能不完整，保留

    for (const block of blocks) {
      const lines = block.split('\n')
      let dataLine = ''

      for (const line of lines) {
        if (line.startsWith('data: ')) {
          dataLine = line.slice(6)
        }
      }

      if (!dataLine) continue

      try {
        const data = JSON.parse(dataLine)
        if (data.event === 'message') {
          this.onMessage(data)
        } else if (data.event === 'message_end') {
          this.onEnd(data)
        } else if (data.event === 'error') {
          this.onError(data)
        }
      } catch {
        // 忽略解析失败的块
      }
    }
  }
}

/**
 * 发送消息并接收 SSE 流式响应
 *
 * @param {Object} params
 * @param {string} params.query - 用户消息
 * @param {string} params.conversationId - 会话 ID（首轮为空）
 * @param {string} params.user - 用户标识
 * @param {Function} params.onChunk - 每收到一段文本时回调
 * @param {Function} params.onEnd - 流结束时回调，参数为 { conversationId, messageId }
 * @param {Function} params.onError - 错误回调
 * @returns {Object} { abort: Function } 可调用 abort() 取消请求
 */
export function sendMessageStream({ query, conversationId, user, onChunk, onEnd, onError }) {
  const parser = new SSEParser(
    (data) => {
      // data.answer 是增量文本
      if (data.answer) {
        onChunk(data.answer, data.conversation_id)
      }
    },
    (data) => {
      onEnd({
        conversationId: data.conversation_id,
        messageId: data.message_id
      })
    },
    (data) => {
      onError(data.message || 'AI 回复出错')
    }
  )

  // #ifdef MP-WEIXIN
  const requestTask = uni.request({
    url: `${API_BASE}/api/chat/stream`,
    method: 'POST',
    data: {
      query,
      conversation_id: conversationId || '',
      user
    },
    header: { 'Content-Type': 'application/json' },
    enableChunked: true,
    success() {
      // 流在 onChunkReceived 中已处理完毕
    },
    fail(err) {
      onError('网络请求失败，请检查网络后重试')
    }
  })

  requestTask.onChunkReceived((res) => {
    const text = new TextDecoder('utf-8').decode(res.data)
    parser.feed(text)
  })

  return {
    abort: () => requestTask.abort()
  }
  // #endif

  // #ifdef H5
  // H5 使用 fetch + ReadableStream（开发调试用）
  // 注意：UniApp 条件编译要求 return 在各自的 #ifdef 块内
  // #endif
}

/**
 * 发送消息并等待完整响应（blocking 模式）
 *
 * @param {Object} params
 * @returns {Promise<{ answer: string, conversationId: string, messageId: string }>}
 */
export async function sendMessageBlocking({ query, conversationId, user }) {
  const response = await uni.request({
    url: `${API_BASE}/api/chat`,
    method: 'POST',
    data: {
      query,
      conversation_id: conversationId || '',
      user
    },
    header: { 'Content-Type': 'application/json' }
  })

  if (response.statusCode !== 200) {
    throw new Error(response.data?.error || '请求失败')
  }

  return {
    answer: response.data.answer,
    conversationId: response.data.conversation_id,
    messageId: response.data.message_id
  }
}
```

- [ ] **Step 2: 验证 API 客户端**

在微信开发者工具中，确保代理服务器正在运行。临时在 `pages/chat/chat.vue` 中测试：

```vue
<template>
  <view class="container">
    <text>API 测试</text>
    <button @click="testAPI">测试 API</button>
    <text>{{ result }}</text>
  </view>
</template>

<script setup>
import { ref } from 'vue'
import { sendMessageBlocking } from '../../api/dify'

const result = ref('')

async function testAPI() {
  try {
    const res = await sendMessageBlocking({
      query: '你好',
      conversationId: '',
      user: 'test_user'
    })
    result.value = res.answer
    console.log('API response:', res)
  } catch (err) {
    result.value = 'Error: ' + err.message
  }
}
</script>
```

需要代理的 `.env` 中配置了有效的 Dify API Key。如果还没有，可以先跳过此验证，直接进入下一步。

- [ ] **Step 3: 提交**

```bash
git add gaokao-miniprogram/src/api/dify.js
git commit -m "feat: add Dify API client with SSE streaming parser"
```

---

### Task 6: ChatBubble 消息气泡组件

**Files:**
- Create: `gaokao-miniprogram/src/components/ChatBubble.vue`

- [ ] **Step 1: 实现 ChatBubble 组件**

```vue
<!-- gaokao-miniprogram/src/components/ChatBubble.vue -->
<template>
  <view class="bubble-wrap" :class="[`bubble-${type}`]">
    <!-- AI 头像 -->
    <view v-if="type === 'ai'" class="avatar">
      <text class="avatar-text">峰</text>
    </view>

    <!-- 气泡内容 -->
    <view class="bubble" :class="[`bubble-${type}-inner`]">
      <text class="bubble-text" :class="{ 'text-streaming': isStreaming }">
        {{ content }}<text v-if="isStreaming" class="cursor" />
      </text>
      <text v-if="type === 'ai'" class="ai-label">AI 生成 · 仅供参考</text>
    </view>
  </view>
</template>

<script setup>
defineProps({
  type: {
    type: String,
    required: true,
    validator: (v) => ['user', 'ai'].includes(v)
  },
  content: {
    type: String,
    default: ''
  },
  isStreaming: {
    type: Boolean,
    default: false
  }
})
</script>

<style lang="scss" scoped>
.bubble-wrap {
  display: flex;
  align-items: flex-start;
  margin-bottom: 24rpx;
  padding: 0 24rpx;
}

.bubble-user {
  justify-content: flex-end;
}

.bubble-ai {
  justify-content: flex-start;
}

.avatar {
  width: 64rpx;
  height: 64rpx;
  background: $brand-primary;
  border-radius: $radius-md;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  margin-right: 16rpx;
}

.avatar-text {
  color: #fff;
  font-size: 28rpx;
  font-weight: bold;
}

.bubble {
  max-width: 75%;
  padding: 20rpx 28rpx;
  line-height: 1.6;
}

.bubble-user-inner {
  background: $brand-primary;
  border-radius: $radius-lg 4rpx $radius-lg $radius-lg;
}

.bubble-ai-inner {
  background: $bg-white;
  border-radius: 4rpx $radius-lg $radius-lg $radius-lg;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.05);
}

.bubble-text {
  font-size: 30rpx;
  word-break: break-word;
}

.bubble-user-inner .bubble-text {
  color: #fff;
}

.bubble-ai-inner .bubble-text {
  color: $text-primary;
}

.cursor {
  display: inline-block;
  width: 4rpx;
  height: 32rpx;
  background: $brand-primary;
  vertical-align: text-bottom;
  margin-left: 4rpx;
  animation: blink 1s step-end infinite;
}

.ai-label {
  display: block;
  font-size: 22rpx;
  color: $text-muted;
  margin-top: 8rpx;
}

@keyframes blink {
  0%, 50% { opacity: 1; }
  51%, 100% { opacity: 0; }
}
</style>
```

- [ ] **Step 2: 在对话页临时验证**

在 `pages/chat/chat.vue` 中临时使用组件：

```vue
<template>
  <view style="padding: 40rpx;">
    <ChatBubble type="ai" content="你好！我是峰哥报考参谋" />
    <ChatBubble type="user" content="我580分能上什么学校？" />
    <ChatBubble type="ai" content="河北物理类580，这个分数段..." :isStreaming="true" />
  </view>
</template>

<script setup>
import ChatBubble from '../../components/ChatBubble.vue'
</script>
```

在微信开发者工具中确认：AI 气泡左对齐有头像，用户气泡右对齐橙色，streaming 状态有闪烁光标。

- [ ] **Step 3: 提交**

```bash
git add gaokao-miniprogram/src/components/ChatBubble.vue
git commit -m "feat: add ChatBubble component with streaming cursor"
```

---

### Task 7: QuickQuestions 快捷问题组件

**Files:**
- Create: `gaokao-miniprogram/src/components/QuickQuestions.vue`

- [ ] **Step 1: 实现 QuickQuestions 组件**

```vue
<!-- gaokao-miniprogram/src/components/QuickQuestions.vue -->
<template>
  <view class="quick-wrap">
    <view
      v-for="(q, index) in questions"
      :key="index"
      class="quick-chip"
      @click="$emit('select', q)"
    >
      <text class="quick-text">{{ q }}</text>
    </view>
  </view>
</template>

<script setup>
defineEmits(['select'])

const questions = [
  '我580分能上什么学校？',
  '计算机还值得报吗？',
  '法学就业怎么样？',
  '帮我分析志愿方案'
]
</script>

<style lang="scss" scoped>
.quick-wrap {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
  padding: 0 24rpx 24rpx;
}

.quick-chip {
  background: $bg-white;
  border: 2rpx solid $brand-primary;
  border-radius: $radius-full;
  padding: 12rpx 28rpx;
}

.quick-text {
  font-size: 26rpx;
  color: $brand-primary;
}
</style>
```

- [ ] **Step 2: 在对话页临时验证**

```vue
<template>
  <view style="padding: 40rpx;">
    <QuickQuestions @select="onSelect" />
  </view>
</template>

<script setup>
import QuickQuestions from '../../components/QuickQuestions.vue'

function onSelect(q) {
  console.log('Selected:', q)
}
</script>
```

确认 4 个气泡按钮显示正确，点击时控制台输出对应问题。

- [ ] **Step 3: 提交**

```bash
git add gaokao-miniprogram/src/components/QuickQuestions.vue
git commit -m "feat: add QuickQuestions component"
```

---

### Task 8: 首页

**Files:**
- Modify: `gaokao-miniprogram/src/pages/index/index.vue`

- [ ] **Step 1: 实现首页完整 UI**

```vue
<!-- gaokao-miniprogram/src/pages/index/index.vue -->
<template>
  <view class="page">
    <!-- 品牌 Header -->
    <view class="header">
      <view class="logo">
        <text class="logo-text">峰</text>
      </view>
      <text class="title">峰哥报考参谋</text>
      <text class="subtitle">张雪峰风格的 AI 志愿填报助手</text>
    </view>

    <!-- 功能描述 -->
    <view class="desc-card">
      <text class="desc-text">问专业、问学校、问分数，AI 帮你分析志愿填报，免费不限次数。</text>
    </view>

    <!-- 核心入口 -->
    <view class="primary-btn" @click="goChat">
      <text class="primary-btn-icon">💬</text>
      <view class="primary-btn-content">
        <text class="primary-btn-title">免费咨询</text>
        <text class="primary-btn-sub">AI 实时对话 · 不限次数</text>
      </view>
    </view>

    <!-- 未来入口 -->
    <view class="future-row">
      <view class="future-card">
        <text class="future-text">📋 测评填表</text>
        <text class="future-hint">即将上线</text>
      </view>
      <view class="future-card">
        <text class="future-text">📊 综合报告</text>
        <text class="future-hint">即将上线</text>
      </view>
    </view>

    <!-- 免责声明 -->
    <view class="disclaimer">
      <text class="disclaimer-text">⚠️ 数据仅供参考，请以各省考试院公布信息为准</text>
    </view>
  </view>
</template>

<script setup>
function goChat() {
  uni.navigateTo({ url: '/pages/chat/chat' })
}
</script>

<style lang="scss" scoped>
.page {
  min-height: 100vh;
  background: linear-gradient(135deg, $brand-gradient-start 0%, $brand-gradient-end 100%);
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 0 32rpx;
  padding-top: 120rpx;
  box-sizing: border-box;
}

.header {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-bottom: 48rpx;
}

.logo {
  width: 128rpx;
  height: 128rpx;
  background: $brand-primary;
  border-radius: $radius-lg;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 24rpx;
}

.logo-text {
  color: #fff;
  font-size: 56rpx;
  font-weight: bold;
}

.title {
  font-size: 40rpx;
  font-weight: 600;
  color: $text-primary;
  margin-bottom: 8rpx;
}

.subtitle {
  font-size: 26rpx;
  color: $text-secondary;
}

.desc-card {
  width: 100%;
  background: $bg-white;
  border-radius: $radius-lg;
  padding: 28rpx 32rpx;
  margin-bottom: 28rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.06);
}

.desc-text {
  font-size: 28rpx;
  color: $text-secondary;
  line-height: 1.7;
}

.primary-btn {
  width: 100%;
  background: $brand-primary;
  border-radius: $radius-lg;
  padding: 32rpx;
  display: flex;
  align-items: center;
  margin-bottom: 20rpx;
}

.primary-btn-icon {
  font-size: 44rpx;
  margin-right: 24rpx;
}

.primary-btn-content {
  display: flex;
  flex-direction: column;
}

.primary-btn-title {
  font-size: 34rpx;
  font-weight: 600;
  color: #fff;
}

.primary-btn-sub {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.85);
  margin-top: 4rpx;
}

.future-row {
  width: 100%;
  display: flex;
  gap: 20rpx;
  margin-bottom: auto;
}

.future-card {
  flex: 1;
  background: $bg-white;
  border-radius: $radius-md;
  padding: 24rpx;
  opacity: 0.45;
  border: 2rpx dashed $border-light;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.future-text {
  font-size: 28rpx;
  color: $text-secondary;
}

.future-hint {
  font-size: 22rpx;
  color: $text-muted;
  margin-top: 4rpx;
}

.disclaimer {
  width: 100%;
  padding: 32rpx 0 48rpx;
  text-align: center;
}

.disclaimer-text {
  font-size: 22rpx;
  color: $text-muted;
}
</style>
```

- [ ] **Step 2: 在微信开发者工具中验证**

确认首页显示完整：渐变背景、Logo、功能描述、橙色大按钮、两个灰色占位卡、底部免责。点击"免费咨询"能跳转到对话页。

- [ ] **Step 3: 提交**

```bash
git add gaokao-miniprogram/src/pages/index/index.vue
git commit -m "feat: implement home page with brand design"
```

---

### Task 9: 对话页

**Files:**
- Modify: `gaokao-miniprogram/src/pages/chat/chat.vue`

这是最核心的页面，包含完整的对话交互逻辑。

- [ ] **Step 1: 实现对话页**

```vue
<!-- gaokao-miniprogram/src/pages/chat/chat.vue -->
<template>
  <view class="chat-page">
    <!-- 对话区域 -->
    <scroll-view
      class="chat-scroll"
      scroll-y
      :scroll-top="scrollTop"
      :scroll-with-animation="true"
    >
      <!-- AI 欢迎语 -->
      <ChatBubble type="ai" :content="welcomeMsg" />

      <!-- 快捷问题（仅首次显示，发送第一条消息后隐藏） -->
      <QuickQuestions v-if="messages.length === 0" @select="onQuickSelect" />

      <!-- 消息列表 -->
      <ChatBubble
        v-for="(msg, index) in messages"
        :key="index"
        :type="msg.role"
        :content="msg.content"
        :isStreaming="isStreaming && index === messages.length - 1 && msg.role === 'ai'"
      />

      <!-- 底部间距 -->
      <view style="height: 24rpx;" />
    </scroll-view>

    <!-- 底部输入栏 -->
    <view class="input-bar">
      <input
        class="input-field"
        v-model="inputText"
        placeholder="输入你想问的..."
        placeholder-class="input-placeholder"
        :disabled="isStreaming"
        confirm-type="send"
        @confirm="onSend"
      />
      <view
        class="send-btn"
        :class="{ 'send-btn-active': inputText.trim() && !isStreaming }"
        @click="onSend"
      >
        <text class="send-icon">↑</text>
      </view>
    </view>
  </view>
</template>

<script setup>
import { ref, nextTick } from 'vue'
import { onShow } from '@dcloudio/uni-app'
import ChatBubble from '../../components/ChatBubble.vue'
import QuickQuestions from '../../components/QuickQuestions.vue'
import { sendMessageStream } from '../../api/dify.js'
import { getUserId, loadHistory, saveHistory, appendMessage, clearHistory } from '../../utils/storage.js'

const welcomeMsg = '同学你好！我是峰哥报考参谋，你的 AI 志愿填报助手。有什么想问的？分数、学校、专业，都可以聊 👋'

const messages = ref([])
const inputText = ref('')
const isStreaming = ref(false)
const scrollTop = ref(0)
const conversationId = ref('')

let currentAbort = null

// 页面显示时恢复历史
onShow(() => {
  const history = loadHistory()
  conversationId.value = history.conversationId
  messages.value = history.messages
})

// 滚动到底部
function scrollToBottom() {
  nextTick(() => {
    scrollTop.value = scrollTop.value === 0 ? 1 : 0
    // 取值变化才能触发滚动，用大值确保到底
    setTimeout(() => {
      scrollTop.value = 999999
    }, 50)
  })
}

// 快捷问题点击
function onQuickSelect(question) {
  inputText.value = question
  onSend()
}

// 发送消息
function onSend() {
  const text = inputText.value.trim()
  if (!text || isStreaming.value) return

  // 添加用户消息
  messages.value = appendMessage(conversationId.value, {
    role: 'user',
    content: text
  })
  inputText.value = ''
  scrollToBottom()

  // 添加空的 AI 消息占位
  const aiMsg = { role: 'ai', content: '', timestamp: Date.now() }
  messages.value = [...messages.value, aiMsg]
  isStreaming.value = true
  scrollToBottom()

  // 发送到 Dify
  currentAbort = sendMessageStream({
    query: text,
    conversationId: conversationId.value,
    user: getUserId(),
    onChunk(answerChunk, convId) {
      // 更新最后一条 AI 消息的 content
      const lastMsg = messages.value[messages.value.length - 1]
      if (lastMsg && lastMsg.role === 'ai') {
        lastMsg.content += answerChunk
        messages.value = [...messages.value] // 触发响应式更新
      }
      if (convId && !conversationId.value) {
        conversationId.value = convId
      }
    },
    onEnd({ conversationId: convId }) {
      isStreaming.value = false
      if (convId) conversationId.value = convId
      // 保存完整对话到历史
      const msgs = messages.value.filter(m => m.content.length > 0)
      saveHistory(conversationId.value, msgs)
      scrollToBottom()
    },
    onError(errMsg) {
      isStreaming.value = false
      // 更新 AI 消息为错误提示
      const lastMsg = messages.value[messages.value.length - 1]
      if (lastMsg && lastMsg.role === 'ai') {
        lastMsg.content = errMsg || '出了点问题，请稍后重试'
        messages.value = [...messages.value]
      }
      scrollToBottom()
    }
  })
}
</script>

<style lang="scss" scoped>
.chat-page {
  display: flex;
  flex-direction: column;
  height: 100vh;
  background: $bg-page;
}

.chat-scroll {
  flex: 1;
  padding-top: 16rpx;
}

.input-bar {
  display: flex;
  align-items: center;
  gap: 16rpx;
  padding: 16rpx 24rpx;
  padding-bottom: calc(16rpx + env(safe-area-inset-bottom));
  background: $bg-white;
  border-top: 2rpx solid $border-light;
}

.input-field {
  flex: 1;
  background: $bg-input;
  border-radius: $radius-full;
  padding: 18rpx 32rpx;
  font-size: 30rpx;
}

.input-placeholder {
  color: $text-muted;
}

.send-btn {
  width: 72rpx;
  height: 72rpx;
  background: $bg-input;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.send-btn-active {
  background: $brand-primary;
}

.send-icon {
  color: $text-muted;
  font-size: 32rpx;
}

.send-btn-active .send-icon {
  color: #fff;
}
</style>
```

- [ ] **Step 2: 在微信开发者工具中验证**

1. 从首页点击"免费咨询"进入对话页
2. 确认 AI 欢迎语显示
3. 确认 4 个快捷问题显示
4. 点击快捷问题，确认消息发送后快捷问题消失
5. 如果代理和 Dify 已配置：确认 AI 回复逐字显示
6. 如果代理未配置：确认错误提示友好
7. 返回首页再进入，确认历史对话恢复

- [ ] **Step 3: 提交**

```bash
git add gaokao-miniprogram/src/pages/chat/chat.vue
git commit -m "feat: implement chat page with SSE streaming and history"
```

---

### Task 10: 端到端联调

**前置条件：** Dify 服务在 8.135.37.159 上运行，API Key 已配置在代理 `.env` 中。

- [ ] **Step 1: 启动代理（本地或服务器）**

```bash
# 本地测试
cd gaokao-proxy
# 编辑 .env: DIFY_API_URL=http://8.135.37.159:8080
npm run dev
```

或部署到服务器：

```bash
# 上传代理到服务器
scp -r gaokao-proxy root@8.135.37.159:/opt/gaokao-proxy/
ssh root@8.135.37.159 "cd /opt/gaokao-proxy && npm install && npm start"
```

- [ ] **Step 2: 更新前端 API 地址**

编辑 `gaokao-miniprogram/src/api/dify.js` 中的 `API_BASE`：

```javascript
// 本地开发
const API_BASE = 'http://localhost:3001'

// 服务器部署后替换为
// const API_BASE = 'https://your-domain.com'
```

- [ ] **Step 3: 运行完整测试流程**

在微信开发者工具中：

1. 打开小程序 → 首页显示正常
2. 点击"免费咨询" → 进入对话页
3. 点击"我580分能上什么学校？" → AI 开始流式回复
4. 等待回复完成 → 光标消失，显示"AI 生成 · 仅供参考"
5. 手动输入"法学怎么样？" → 发送 → 收到回复
6. 返回首页 → 再次进入 → 历史对话恢复
7. 断开网络 → 发送消息 → 显示友好错误提示

- [ ] **Step 4: 提交最终版本**

```bash
git add -A
git commit -m "feat: MVP chat page complete with SSE streaming"
```

---

## Self-Review

**Spec coverage:**
- ✅ 首页（品牌元素、免费咨询入口、灰色占位、免责声明）→ Task 8
- ✅ 对话页（欢迎语、快捷问题、气泡、打字机效果、AI 标识）→ Task 6, 7, 9
- ✅ SSE 流式 + Node 代理 + API Key 保护 → Task 3, 5
- ✅ conversation_id 多轮上下文 → Task 5, 9
- ✅ 本地对话历史存储 → Task 4
- ✅ 错误处理 → Task 3, 5
- ✅ 项目结构 → Task 1

**Placeholder scan:** 无 TBD/TODO。`__YOUR_APPID__` 和 `API_BASE` 标注为需要替换，属正常配置占位。

**Type consistency:** `sendMessageStream` 返回 `{ abort }` 在 Task 5 定义，Task 9 使用 `currentAbort = sendMessageStream(...)` 一致。`appendMessage` 返回 messages 数组，Task 9 中 `messages.value = appendMessage(...)` 一致。`SSEParser.feed()` 接收 string，`TextDecoder.decode()` 返回 string，一致。
